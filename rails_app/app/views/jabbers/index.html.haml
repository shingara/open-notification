#block-tables.block

  .content
    .inner
      %table.table
        %thead
          %tr
            %th.first Id
            %th To
            %th Body
            %th Requested at
            %th.last Send at
        %tbody
          - @jabbers.each do |jabber|
            %tr{:class => "#{cycle('odd','even')} jabber" }
              %td 1
              %td= jabber.to
              %td= jabber.text
              %td= jabber.created_at
              %td= jabber.send_at

      = will_paginate @jabbers

      .actions-bar
        .actions
          = link_to 'New', new_jabber_url

